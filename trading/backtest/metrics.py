"""Phase 2 stub.

Will compute summary statistics from a completed backtest's fills (read via
`persistence.repository`): total return, win rate, profit factor, max
drawdown, Sharpe ratio, average R-multiple, and expectancy. Output feeds
`validation/gate.py` (paper mode) and is also surfaced in backtest reports.
"""
