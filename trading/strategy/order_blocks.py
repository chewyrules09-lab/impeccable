"""Phase 1 stub.

Will implement Order Block (OB) detection: the last opposing candle
immediately preceding a strong displacement move, used as a value/entry zone
on retest. Parameterized by what counts as "strong displacement" (e.g. a
multiple of average true range or recent candle bodies).
"""
